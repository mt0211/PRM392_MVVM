<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent"/> <!-- Change to your desired color -->
            <corners android:radius="10dp"/> <!-- Rounded corners -->
            <padding android:left="0dp" android:right="0dp" android:top="0dp" android:bottom="4dp"/> <!-- Padding -->
        </shape>
    </item>
</layer-list>
